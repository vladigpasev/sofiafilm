import { BrowserModule } from '@angular/platform-browser';
import { NgModule } from '@angular/core';

@NgModule({
  imports: [
    BrowserModule  // If you're using Angular routing or need this setup
  ],
  providers: [],
})
export class AppModule {}
